import { Logo } from '../../components/Logo'
import './default.css'

const Default = () => {
  return (
    <header className="header-default">
      <Logo />
      <p>Header default</p>
    </header>
  )
}

export default Default